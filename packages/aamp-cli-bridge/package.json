{
  "name": "@zengxingyuan/aamp-cli-bridge",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "version": "0.1.7-dev.5",
  "description": "Bridge direct CLI agents into the AAMP email network with profile-driven command invocation",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "aamp-cli-bridge": "dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "prepare-bin": "node -e \"require('node:fs').chmodSync('dist/index.js', 0o755)\"",
    "prepack": "npm run build && npm run prepare-bin",
    "prepublishOnly": "npm run build && npm run prepare-bin",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "aamp-sdk": "npm:@luckyterry/aamp-sdk@0.1.24-dev.1",
    "qrcode-terminal": "^0.12.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/qrcode-terminal": "^0.12.2",
    "tsx": "^4.7.1",
    "typescript": "^5.4.3"
  }
}
